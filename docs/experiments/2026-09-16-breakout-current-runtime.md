# Breakout action-width candidate on the qualified block runtime

The next Breakout comparison now has current-runtime adapters: source `8092790`
passes **742 Python tests** while reusing the qualified native `886bae68` unchanged.
This is preparation, not a four-action GPU pass or learning result. Pong root 2017
continues on its own immutable bundle; no second GPU job or follower is launched.

The [original diagnostic](2026-09-11-breakout-diagnostic.md) remains the reason
for this experiment: the trained policy improves over its untrained control but
clears neither wall, despite substantial rewarded experience and a late plateau.
Compare the published eighteen-action vocabulary with NOOP/FIRE/RIGHT/LEFT before
adding exploration assistance or extending the budget. Keep the two-wall / 864
point competence gate unchanged. Any successful pilot still needs fresh roots.

## Completed adapter carry

The isolated `/x/Code/.kindle-breakout-current-20260916` worktree carries only the
five Python verification/test files from the previous `0591eda` candidate. It
does not carry that candidate's old ce80 backend or restart its retired follower.
All **32 native/build inputs** and the six packaged Python modules match qualified
`8dc0b98` exactly. Actual package/runner/auditor/replay/scorer imports are checked
in a fresh process. The existing GvkNxZ package is reused, not rebuilt or replaced.

The [completed CPU check](../../runs/breakout-current-check-20260916.ROZlCW/capture/result.json)
binds **5,187 inputs** and preserves eleven complete command records. All 742 tests
pass without failures or skips. This includes real-ALE replay fixtures, not a CPU
learning path. The read-only audit passes; preserve the completed writer.

Published full-action replay retains v1, including historical declarations.
Minimal-action Breakout requires explicit replay v2, action count 4 and the exact
ordered vocabulary. Replay reconstructs the declared ALE action space, and complete
checkpoint inspection rejects a different action schema even if a supplied tensor
template happens to have equal shapes. No collection or learning arithmetic changes.

Fresh upstream reads still find Meganeura `986f49a` and Blade `92553493`; the
qualified runtime retains `589d73ab` / `2accfeee` with matched initialization
observations. All active Pong pins reverify before and after this one-core,
2 GiB / zero-swap preparation. No NVML or GPU query is used by the CPU check.

## Native fixtures and remaining gates

The separate `/x/Code/.kindle-breakout-current-gates-20260916` source `3c4f87f`
carries the existing four-action fixtures onto that parent. Production library
code, dependencies and Python are unchanged. The additions are test-only world/
behavior comparisons and the synthetic canary's `--actions` option, whose default
remains eighteen. The current-driver assertions and initialization observations
stay in place. Native Vulkan samples explicitly check estimated budget headroom,
not physical free or peak memory; no NVML sampling is introduced.

Its [CPU build declaration](../../runs/breakout-current-fixtures-20260916.KA1kRO/declaration.md)
prepares a private release test executable and canary, without a GPU invocation.
All seventeen build commands now pass: **109 workspace/all-target CPU tests**,
formatting, release Clippy with warnings denied, three ignored-test listings and
two invalid-count canary refusals before GPU construction. The original writer's
final checker nevertheless fails: Cargo names a package `blade-graphics`, while
rustc names its dependency file `blade_graphics`. Preserve that failure and its
missing artifact/success result; no source fix or rebuild was performed.

The [separate completion](../../runs/breakout-current-fixture-check-20260916.pzWbk8/capture/result.json)
normalizes only that filename convention and verifies both actual compiler chains,
the shared Blade edge, resolved sources and executable build windows. Two reader
tests cover seven filename cases. It binds **5,567 pins**, preserves all seventeen
original command records and rechecks active Pong inputs. The test executable is
`27a73545`, the canary `f7131dc0`; neither is the Python extension. The independent
read-only audit passes. Preserve both terminal writers and the private target.

All **27 GPU tests remain ignored**. The carried comparisons retain their contract: full
B16/T64 recurrence for the world comparison; 15,360 imagined / 1,008 replay rows
for actor/value heads; loss tolerance 3e-4 and all-gradient relative-L2 tolerance
3e-3 with the existing 1e-7 absolute allowance. Do not relax a failed tolerance.

After the declared Pong campaign, separately declare and individually review the
four/eighteen-action GPU comparisons, complete state from update one and repeats,
zero-update initialization/restore, action-schema rejection and N6 pixel/replay/
episode-stopping checks. Require unchanged eighteen-action controls, exact
same-width state/report/trace comparisons and common-shape initial parameters
across widths. Keep >=2 GiB **native estimated budget headroom** in the new
non-NVML declaration; preserve old directly-free-memory gates as historical.

Only then run the paired learning pilot on one source-matched bundle, changing
the action vocabulary alone. Neither the old eighteen-action learning pilot nor
the retired ce80 fixtures constitute its matched control. The new fixture binary
is not a replacement Python package. No action-width adoption, speedup, Breakout
win or new five-game reliability claim follows from preparation.
